{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core + plugin permissions for the Spooky-Pass main window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "autostart:default",
    "opener:default"
  ]
}
